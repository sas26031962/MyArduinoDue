Подключение дисплея **GC9A01** к **Arduino Due** и пример программы:

## 🔌 Подключение GC9A01 к Arduino Due

### **Схема подключения:**
| GC9A01 | Arduino Due | Назначение |
|--------|-------------|------------|
| VCC    | 3.3V        | Питание 3.3V |
| GND    | GND         | Земля |
| SCL    |             | SCK SPI  |-------->Колодка SPI
| SDA    |             | MOSI SPI |-------->Колодка SPI
| RES    | Digital 9   | Сброс |
| DC     | Digital 8   | Команда/Данные |
| CS     | Digital 10  | Выбор чипа |
| BL     | 3.3V        | Подсветка |

---

## 📦 Установка библиотек

1. **Установите библиотеку Adafruit GFX:**
   - В Arduino IDE: **Скетч → Подключить библиотеку → Управлять библиотеками**
   - Найдите "Adafruit GFX Library" и установите

2. **Установите библиотеку для GC9A01:**
   - Скачайте [библиотеку GC9A01](https://github.com/adafruit/Adafruit_GC9A01A)
   - В Arduino IDE: **Скетч → Подключить библиотеку → Добавить .ZIP библиотеку**

---

## 💻 Пример программы

```cpp
#include <Adafruit_GFX.h>
#include <Adafruit_GC9A01A.h>

// Определяем пины для Arduino Due
#define TFT_CS   10
#define TFT_DC   8
#define TFT_RST  9

// Инициализация дисплея
Adafruit_GC9A01A tft(TFT_CS, TFT_DC, TFT_RST);

void setup() {
  Serial.begin(115200);
  Serial.println("GC9A01 Тест запущен!");
  
  // Инициализация дисплея
  if (!tft.begin()) {
    Serial.println("Ошибка инициализации GC9A01!");
    while (1);
  }
  
  Serial.println("Дисплей GC9A01 инициализирован");
  
  // Поворот дисплея (0, 1, 2, 3)
  tft.setRotation(2);
  
  // Заполняем экран черным
  tft.fillScreen(GC9A01A_BLACK);
  delay(500);
  
  // Тест графики и текста
  testDrawShapes();
  testText();
}

void loop() {
  // Анимация меняющихся кругов
  for (int i = 0; i < 240; i += 10) {
    tft.fillCircle(i, 120, 20, random(0xFFFF));
    delay(50);
  }
  
  tft.fillScreen(GC9A01A_BLACK);
  delay(500);
}

void testDrawShapes() {
  // Рисуем прямоугольник
  tft.drawRect(10, 10, 100, 50, GC9A01A_RED);
  
  // Заливаем прямоугольник
  tft.fillRect(120, 10, 100, 50, GC9A01A_BLUE);
  
  // Рисуем круг
  tft.drawCircle(60, 120, 30, GC9A01A_GREEN);
  
  // Заливаем круг
  tft.fillCircle(180, 120, 30, GC9A01A_YELLOW);
  
  // Рисуем треугольник
  tft.drawTriangle(30, 180, 60, 220, 90, 180, GC9A01A_CYAN);
  
  delay(2000);
  tft.fillScreen(GC9A01A_BLACK);
}

void testText() {
  tft.setCursor(0, 0);
  
  // Разный размер текста
  tft.setTextColor(GC9A01A_WHITE);
  tft.setTextSize(1);
  tft.println("Размер 1: Arduino Due");
  
  tft.setTextColor(GC9A01A_GREEN);
  tft.setTextSize(2);
  tft.println("Размер 2: GC9A01");
  
  tft.setTextColor(GC9A01A_RED);
  tft.setTextSize(3);
  tft.println("Размер 3: 240x240");
  
  tft.setTextColor(GC9A01A_BLUE);
  tft.setTextSize(4);
  tft.println("SPI");
  
  delay(3000);
  tft.fillScreen(GC9A01A_BLACK);
  
  // Вывод различных данных
  tft.setTextSize(2);
  tft.setTextColor(GC9A01A_WHITE);
  tft.setCursor(0, 0);
  
  tft.print("Напряжение: ");
  tft.println(3.3, 1);
  
  tft.print("Время: ");
  tft.println(millis());
  
  delay(2000);
}
```

---

## 🔧 Дополнительные функции

### **Расширенный пример с графиками:**
```cpp
void drawGraph() {
  tft.fillScreen(GC9A01A_BLACK);
  
  // Оси координат
  tft.drawFastVLine(20, 20, 200, GC9A01A_WHITE);
  tft.drawFastHLine(20, 220, 200, GC9A01A_WHITE);
  
  // График синуса
  for (int x = 0; x < 200; x++) {
    int y = 120 + 100 * sin(x * 0.05);
    tft.drawPixel(20 + x, y, GC9A01A_RED);
  }
  
  // График косинуса
  for (int x = 0; x < 200; x++) {
    int y = 120 + 100 * cos(x * 0.05);
    tft.drawPixel(20 + x, y, GC9A01A_BLUE);
  }
}
```

---

## ⚠️ Решение проблем

1. **Дисплей не включается:**
   - Проверьте подключение питания 3.3V
   - Убедитесь, что все GND подключены

2. **Нет изображения:**
   - Проверьте пины RES, DC, CS
   - Убедитесь в правильности SPI подключения

3. **Артефакты на дисплее:**
   - Добавьте конденсатор 100мкФ между VCC и GND
   - Укоротите провода

4. **Медленная работа:**
   - Увеличьте скорость SPI в коде (если поддерживается библиотекой)

---

## 🎯 Особенности Arduino Due

- **SPI работает на 3.3V** - идеально совместимо с GC9A01
- **Высокая производительность** - плавная анимация
- **Не требует преобразователей уровней**

Теперь вы можете использовать дисплей GC9A01 с Arduino Due! Для более сложных проектов изучите дополнительные функции библиотеки Adafruit GFX.
